# 👨‍💻 Настройка нескольких администраторов

## ✅ Изменения выполнены

Бот теперь поддерживает **нескольких администраторов**!

### 📝 Что изменено:

1. **Конфигурация** (`app/core/config.py`):
   - `admin_id` → `admin_ids` (список)
   - Поддержка множественных ID через запятую

2. **Проверка прав** (`app/handlers/admin.py`):
   - Функция `is_admin()` проверяет вхождение в список

3. **Уведомления** (`app/services/notification.py`):
   - Все уведомления отправляются всем админам

---

## 🚀 Как обновить .env файл

Открой файл `.env` и **измени** строку:

**Было:**
```bash
ADMIN_ID=8141463258
```

**Стало:**
```bash
ADMIN_IDS=8141463258,849348909
```

> **Важно:** Используй `ADMIN_IDS` (множественное число) и раздели ID запятой без пробелов

---

## 🔄 Перезапуск бота

После изменения `.env` перезапусти бота:

```bash
# Если бот запущен через Docker
docker-compose -f docker-compose.prod.yml restart

# Или если запущен напрямую
pkill -f "python main.py"
python main.py
```

---

## ✅ Проверка

Оба админа (ID: **8141463258** и **849348909**) теперь могут:
- Открывать `/admin` панель
- Просматривать заявки
- Рассылать сообщения
- Управлять пользователями
- Видеть статистику

---

## 📋 Список текущих администраторов

1. **8141463258** - @pavel_xdev (Главный админ)
2. **849348909** - (Новый админ)

Чтобы добавить больше админов, просто добавь их ID через запятую:
```bash
ADMIN_IDS=8141463258,849348909,123456789
```
